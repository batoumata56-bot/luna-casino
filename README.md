# LUNA Casino

Casino fictif (React + Vite + TypeScript) : argent LC, crypto simulée, 6 jeux, classements et profils personnalisables.

## Lancer

```bash
npm install
npm run dev
```

Tout est stocké dans `localStorage` — aucun argent réel.
